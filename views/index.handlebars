<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao NodeApp</h1>
  <p class="lead">Esse é um BLOG desenvolvido com Node.js + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui você pode ler sobre as novidades da computação.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes: </h2>

{{#each posts}}
<div class="card mt-4">
    <div class="card-body mt-2">
        <h3>{{titulo}}</h3>
        <p>Descrição: {{descricao}}</p>
        <a href="/post/{{slug}}"><button class="btn btn-info">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{#formatDate data}}{{/formatDate}}</small>
    </div>
</div>
{{else}}
    <h5>Ainda não há postagens!</h5>
{{/each}}