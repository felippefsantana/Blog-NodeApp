<div class="card">
    <div class="card-body">
        <h1>{{post.titulo}}</h1>
        <hr>
        <small>Data de publicação: {{#formatDate post.data}}{{/formatDate}}</small><br>
        <small>Categoria: {{post.categoria.nome}}</small>
        <hr>
        {{post.conteudo}}

    </div>
</div>